mixin m_card(name, image, text, date, href)
  .m-card
    .m-card__overlay
    if image
      img.m-card__image(src!= image  alt="")
    .m-card__info
      .m-card__col
        .m-card__title.a-font_text-m!= name
        if date
          .m-card__date!= date
        .m-card__text!= text
      if href
        .m-card__col
          a.m-card__button.button.button--brown(href= href, title= link) Подробнее